INCLUDE_DIRECTORIES(${ZNZLIB_SOURCE_DIR})

SET(NIFTILIB_INCS nifti1_io.h nifti1.h)

SET(NIFTILIB_SRCS nifti1_io.c)

ADD_LIBRARY(niftiio ${NIFTILIB_SRCS} ${NIFTILIB_INCLUDES})
INSTALL_FILES(/include FILEs ${NIFTILIB_INCLUDES})

